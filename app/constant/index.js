const ROLE = require('./ROLE');
const REG_EXP = require('./REG_EXP');
const ERROR = require('./ERROR');
const WS_TYPE = require('./WS_TYPE');

module.exports = {
  ROLE,
  REG_EXP,
  ERROR,
  WS_TYPE,
};
